import React from 'react';
import { shallow } from 'enzyme';
import AddMember from '../../client/src/js/components/userAddMember.jsx';

describe('PostIt-app', () => {
  it('expects App component to be defined', () => {
    expect(AddMember).toBeDefined();
  });
});
